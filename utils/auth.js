// const HEADER_REGEX = /x-token-(.*)$/;

// module.exports = async ({ headers: { authorization } }, Users) => {
//   const email = authorization && HEADER_REGEX.exec(authorization)[1];

//   return email && (await Users.findOne({ email }));
// };
